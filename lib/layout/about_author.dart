import 'package:jaspr/html.dart';

class AboutAuthor extends StatelessComponent {
  const AboutAuthor({super.key});

  @override
  Iterable<Component> build(BuildContext context) sync* {
    yield div([
      
    ]);
  }
}
